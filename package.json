{
  "name": "starwars-webapp",
  "version": "0.1.0",
  "homepage": "https://tomasmejia.github.io/starwars-webapp/",
  "private": true,
  "homepage": "https://tomasmejia.github.io/starwars-webapp/",
  "dependencies": {
    "gh-pages": "^1.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "tachyons": "^4.9.1"
  }
}
